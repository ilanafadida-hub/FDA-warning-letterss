# Optional: Add your OpenAI API key for GPT-powered summarization and Q&A
# Without this key, the system uses rule-based extraction (still works well)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-xxxxx
